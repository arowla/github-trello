oauth_token: [ENV['TRELLO_OAUTH_TOKEN']]
api_key: [ENV['TRELLO_API_KEY']]
board_ids:
  [GSA-OCSIT/hyabusa]: [5tAYyMQv]
on_start:
  move_to: [list id]
  archive: true
on_close:
  move_to: [list id]
  archive: true
# Alternatively, you can specify move_to as a hash of repo name -> list id
#on_close/on_start:
#  move_to:
#    [repo name]: [list id]

# See README for deployment usage
on_deploy:
  move_to:
    [repo name]: [list id]
  archive: true
